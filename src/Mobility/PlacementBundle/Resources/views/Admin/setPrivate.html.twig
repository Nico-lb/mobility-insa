{% extends "MobilityPlacementBundle:Admin:layout.html.twig" %}

{% block page_title %}{{ parent() }} - Rendre privées{% endblock %}

{% block content_body %}
    <div class="text-center">
        <p>
        	Vous êtes sur le point de rendre les affectations de l'année {{ year }} privées.
        </p>
        
        <p>
            <a href="{{ path('placements_setprivate_confirm', {'year': year, 'token': csrf_token('setprivate_placements')}) }}" class="btn btn-danger">Confirmer</a>
            <a href="{{ path('placement_list_year', {'year': year}) }}" class="btn btn-default">Annuler</a>
        </p>
    </div>
{% endblock %}
